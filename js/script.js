var tabProducto = [];
var codigo = Number;
var nombre = String;
var descripcion = String;
var precio = Boolean;
var marca = String;

function Product(element){
    agregar[cod,nom,desc,pre,marc] = tabProducto [
    {codigo: cod , nombre: nom, descripcion: desc, precio: pre, marca: marc}
    ]
}
function AggProducto(agregar){
    var agregar= [
        {
            agregar : document.getElementById("codigo").value,
            agregar : document.getElementById("nombre").value,
            agregar : document.getElementById("descripcion").value,
            agregar : document.getElementById("precio").value,
            agregar : document.getElementById("marca").value,
        }
    ];
   alert(agregar);
   this.agregar.push(Product.insertElementById(agregarProducto));

}
